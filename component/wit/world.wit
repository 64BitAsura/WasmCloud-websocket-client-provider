package wasmcloud:custom;

world component {
  // Import logging for processing data
  import wasi:logging/logging@0.1.0-draft;

  // Import the WebSocket message-handler interface
  // This allows the component to receive messages from the provider
  export wasmcloud:websocket/message-handler;
}
